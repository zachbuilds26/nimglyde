# Backend URL the Mini App talks to. Local dev default below.
# On your phone via Nimiq Pay Custom URL, set this to your computer's LAN
# address, e.g. VITE_BACKEND_URL=http://192.168.1.42:8787 then restart dev.
VITE_BACKEND_URL=http://localhost:8787
